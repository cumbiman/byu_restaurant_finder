{
  "name": "byu_restaurant_finder",
  "version": "1.0.0",
  "description": "Find open restaurants",
  "scripts": {
    "start": "func start",
    "test": "jest",
    "test-coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.1"
  }
}
